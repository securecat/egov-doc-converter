# Changelog

## [1.0.7] - 2026-09-18

### 修正

- 成功・失敗ステータスメッセージのボーダー色（`--success-border`/`--danger-border`）に、より余裕を持ったコントラスト比の色を採用
  - 変換結果カード内（背景`#EEEEEE`）・「ファイルを選択」パネル内（背景`#FFFFFF`）のどちらに表示されても3:1を十分に上回るよう調整

## [1.0.6] - 2026-09-18

### 修正

- ライトテーマで背景色とほぼ同化していた要素にボーダーを追加・変更し、境界を判別できるように修正
  - 変換結果カードの外周のボーダー色を変更
  - 変換結果カードの背景色を薄いグレー（`--result-surface`）に変更し、白背景に近い各種メッセージボックスとの区別を確保
  - 成功・失敗・PDF生成中の各ステータスメッセージボックスにボーダーを追加

## [1.0.5] - 2026-09-18

### 修正

- 「装飾的な区切り線」と「UIコンポーネントの境界」を同じCSS変数（`--border`）で兼用していた設計を修正し、役割ごとに変数を分離
  - タブ下の区切り線 → 装飾用の`--border`として残し、ライト・ダークとも単独でコントラスト比3:1以上を確保するよう値を調整
  - 変換結果カード・プレビュー枠の外周 → コンポーネント境界用の`--border-strong`に統一

## [1.0.4] - 2026-09-18

### 修正

- ダークテーマで背景色とほぼ同化していた要素にボーダーを追加し、境界を判別できるように修正
  - 「HTMLに変換して表示」ボタン（白ボーダー）
  - 変換結果カードの外周（`.tabs`の下線など同じ変数を使う箇所も含む）
  - 成功・失敗・処理中の各ステータスメッセージボックス
- ボタンのネイティブ表示（`appearance`）をリセットし、ホバー時の見た目のずれを修正

## [1.0.3] - 2026-09-18

### 修正

- ダークテーマ時にコントラスト比が不足していたアクセント色（見出し・ボタン等）を修正
  - 文字・境界線用の役割と、白文字を乗せる塗りボタン用の役割を別トークンに分離
  - 塗りボタンにはダークテーマ時に判別できる境界線を追加
- `font-family` を `sans-serif` のみに変更（日本語フォント名の直接指定をやめ、OS標準フォントに委ねる）

## [1.0.2] - 2026-09-18

### 修正

- 16px未満だった文字サイズをすべて16px以上に修正
- ダークテーマ非対応だった箇所（カード・ドロップゾーンの枠線、ホバー・フォーカス時の強調色、注釈テキストの色など）を修正
- `font-family` から `system-ui` の指定を削除

## [1.0.1] - 2026-09-18

### 修正

- CDN経由で読み込む外部スクリプト（html2canvas・jsPDF・JSZip）に `defer` 属性を追加

## [1.0.0] - 2026-09-18

### 追加

- XML + XSLファイル（e-Gov等のZIP同梱）をHTML／PDFに変換する機能
- CSVファイルをカード・表形式に整形して表示する機能
